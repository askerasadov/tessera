package io.lightine.tessera.mrz

import kotlin.test.Test
import kotlin.test.assertEquals

/**
 * Smoke test that exists solely to verify the test infrastructure runs.
 * Real tests are written alongside implementation, not as scaffolding.
 */
class TestInfrastructureSmokeTest {
    @Test
    fun test_infrastructure_runs_a_passing_test() {
        assertEquals(2, 1 + 1)
    }
}
